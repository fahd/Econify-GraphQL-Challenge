const locationType = `
  type Location {
    id: ID!
    name: String!
    address: String!
    latitude: String!
    longitude: String!
    createdAt: DateTime!
    updatedAt: DateTime!
  }
`;

export default locationType;
